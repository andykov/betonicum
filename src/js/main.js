

// import './modal';
import './sidebar';

import $ from 'jquery';

// import './modal2';



$(document).ready(() => {
  const str = `window location is ${window.location}`;
  // eslint-disable-next-line no-console
  console.log(str);
});
